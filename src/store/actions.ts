import {ActionType, StateType, UseActionType} from './types';
import {Dispatch} from "react";

export default function useActions(state: StateType, dispatch: Dispatch<ActionType>): UseActionType {
    return {};
}
